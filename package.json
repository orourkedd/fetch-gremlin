{
  "name": "fetch-gremlin",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "transpile": "babel src --out-dir lib --source-maps",
    "deploy": "npm run transpile && npm test && npm publish; rm -rf lib",
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fetch-everywhere": "^1.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-jest": "^20.0.0",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^20.0.4"
  }
}
